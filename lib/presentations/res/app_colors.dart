import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // Main colors
  static const Color primaryColor = Color(0xFF2353E5);
  static const Color secondaryColor = Color(0xFFFFC107);

  // Light Mode
  static const Color lightBackground = Color(0xFFF5F5F5);
  static const Color lightCard = Color(0xFFFFFFFF);
  static const Color lightText = Color(0xFF212121);

  // Dark Mode
  static const Color darkBackground = Color(0xFF121212);
  static const Color darkCard = Color(0xFF1E1E1E);
  static const Color darkText = Color(0xFFE0E0E0);

  // Background colors
  static const Color backgroundColor = Color(0xFFF5F5F5);
  static const Color cardBackgroundColor = Color(0xFFFFFFFF);

  // Text colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);

  // State colors
  static const Color errorColor = Color(0xFFD32F2F);
  static const Color successColor = Color(0xFF4CAF50);
  static const Color warningColor = Color(0xFFFFA000);

  // Neutral Colors and Miscellaneous
  static const Color dividerColor = Color(0xFFBDBDBD);
  static const Color iconColor = Color(0xFF616161);
  static const Color shadowColor = Color(0xFFE0E0E0);

  // Hover and Selection colors
  static const Color hoverColor = Color(0xFFEBF2FF);
  static const Color selectedColor = Color(0xFFDBE1FF);
}
